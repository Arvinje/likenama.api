<div class="col-lg-12">
  <h1 class="text-primary" style=" font-size: 50px;"><span class="glyphicon glyphicon glyphicon-file" style="line-height: 30px;"></span> مشخصات‌کاربر</h1>
</div>
<div class="col-lg-12">
  <div class="row">
    <div class="col-sm-4 col-sm-offset-1">
      <h2><span class="glyphicon glyphicon-user"></span><span class="english"> <%= @user.username %></span></h2>
      <div class="row">
        <div class="col-xs-12">
          <h3><span class="glyphicon glyphicon-heart"></span> <%= @user.like_credit %></h3>
          <h3><span class="glyphicon glyphicon-piggy-bank"></span> <%= @user.coin_credit %></h3>
          <%= render partial: 'shared/management/controls/lock', locals: { user: @user } %>
        </div>
      </div>
    </div>
    <div class="col-sm-6">
      <h3><span class="glyphicon glyphicon-calendar"></span> تاریخ عضویت: <%= @user.created_at.to_parsi.strftime("%-d %B %Y در %k:%M:%S") %></h3>
      <h3><span class="glyphicon glyphicon-time"></span> ورود حاضر در <%= @user.current_sign_in_at.to_parsi.strftime("%-d %B %Y در %k:%M:%S") if @user.current_sign_in_at %></h3>
      <h3><span class="glyphicon glyphicon-globe"></span> آی‌پی حال کاربر <%=  @user.current_sign_in_ip %></h3>
      <h3><span class="glyphicon glyphicon-time"></span> آخرین ورود در <%= @user.last_sign_in_at.to_parsi.strftime("%-d %B %Y در %k:%M:%S") if @user.last_sign_in_at %></h3>
      <h3><span class="glyphicon glyphicon-globe"></span> آی‌پی آخرین ورود <%= @user.last_sign_in_ip %></h3>
    </div>
  </div>
</div>
<div class="col-sm-10 col-sm-offset-1">
  <div class="row">
    <div class="col-xs-12">
      <h3><span class="glyphicon glyphicon-th-list"></span> کمپین‌های کاربر</h3>
    </div>
    <div class="col-xs-6">
      <div class="dropdown" id="campaign-type-dropdown">
        <button class="btn btn-default dropdown-toggle" type="button" id="campaignsdropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="true">
          آخرین کمپین‌ها
          <span class="caret"></span>
        </button>
        <ul class="dropdown-menu" aria-labelledby="campaignsdropdown">
          <li><a href="#">آخرین کمپین‌ها</a></li>
          <li><a href="#">درحال نمایش</a></li>
          <li><a href="#">درانتظار تایید</a></li>
          <li><a href="#">تمام‌شده</a></li>
          <li><a href="#">رد شده</a></li>
        </ul>
      </div>
    </div>
  </div>
  <div class="row">
    <div class="col-xs-12">
      <table class="table table-hover">
        <thead>
          <tr>
            <th>شماره کمپین</th>
            <th>نوع کمپین</th>
            <th>نوع پرداخت</th>
            <th>ساخته‌شده در</th>
            <th >وضعیت</th>
          </tr>
        </thead>
        <tbody>
          <%= render partial: 'shared/management/rows/campaign', collection: @user.campaigns %>
        </tbody>
      </table>
    </div>
  </div>
</div>
